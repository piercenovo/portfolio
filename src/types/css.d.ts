// Types global stylesheet imports (e.g. `import '@/app/globals.css'` in RootDocument).
// Editors on newer TypeScript check side-effect imports by default
// (`noUncheckedSideEffectImports`) and flag CSS files without a declaration.
declare module '*.css'
